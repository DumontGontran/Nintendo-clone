import Header from '../components/Header.component'
import SideMenu from '../components/SideMenu.component';

const Home = () => {
    return (
        <main>
            <Header />
            <SideMenu />
        </main>
    );
};

export default Home;